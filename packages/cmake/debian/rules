#!/usr/bin/make -f

NAME=cmake

clean:
	rm -rf ${CURDIR}/debian/${NAME}

build:
	./bootstrap --system-curl
	make -j$(shell nproc)

binary:
	make install DESTDIR=${CURDIR}/debian/${NAME}
	dh_shlibdeps
	dh_gencontrol
	dh_builddeb
	
binary-arch:
	dpkg --print-architecture
